# Plataforma de Eventos e Inscripciones

API REST desarrollada con **Node.js, Express y MongoDB**, organizada mediante una arquitectura por capas para facilitar el mantenimiento, la escalabilidad y la incorporación progresiva de nuevas funcionalidades.

El proyecto corresponde a una **Plataforma de Eventos e Inscripciones**, donde los usuarios pueden registrarse, iniciar sesión y consultar su información autenticada mediante **JWT almacenado en una cookie HTTP Only**.

Esta entrega incorpora el sistema de autenticación de usuarios mediante JWT, cookies, middleware de autenticación y logout.

---

# Tecnologías utilizadas

- **Node.js** – Entorno de ejecución de JavaScript.
- **Express.js** – Framework para el desarrollo de APIs REST.
- **MongoDB** – Base de datos NoSQL.
- **Mongoose** – ODM para MongoDB.
- **Passport.js** – Middleware de autenticación.
- **Passport-Local** – Estrategia para registro y autenticación mediante email y contraseña.
- **Passport-JWT** – Estrategia para validar el JWT almacenado en una cookie.
- **JSON Web Token (JWT)** – Autenticación mediante tokens.
- **Bcrypt** – Hash y comparación segura de contraseñas.
- **Cookie-parser** – Lectura y gestión de cookies.
- **Dotenv** – Gestión de variables de entorno.
- **Nodemon** – Reinicio automático del servidor durante el desarrollo.
- **JavaScript ES Modules** – Sistema de módulos mediante `import` / `export`.

---

# Instalación

## 1. Clonar el repositorio

```bash
git clone https://github.com/Liliana993/arquitectura_backend.git
```

## 2. Ingresar al proyecto

```bash
cd arquitectura_backend
```

## 3. Instalar dependencias

```bash
npm install
```

---

# Configuración

Crear un archivo **.env** utilizando como base **.env.example**.

Ejemplo:

```env
PORT=8080
MONGO_URL=tu_url_de_mongodb
JWT_SECRET=tu_clave_secreta
JWT_EXPIRES_IN=1h
NODE_ENV=development
```

### Variables de entorno

| Variable     | Descripción                                             |
|--------------|---------------------------------------------------------|
| PORT         | Puerto donde se ejecuta el servidor.                    |
| MONGO_URL    | URL de conexión a MongoDB (implementación futura).      |
| JWT_SECRET   | Clave secreta utilizada para firmar y verificar los JWT.|
|JWT_EXPIRES_IN| Tiempo de expiración del JWT.                           |
| NODE_ENV     | Entorno de ejecución de la aplicación.                  |

---

# Ejecución

## Modo desarrollo

```bash
npm run dev
```

El servidor iniciará utilizando el puerto configurado en el archivo `.env`.

---

# Estructura del proyecto

```text

├── src/
│   ├── config/
│   │   ├── passport.config.js   # Configuración y estrategias de Passport
│   │   └── databese.js          # Configuraciones conexión DB
│   │
│   ├── controllers/             # Controladores de la aplicación
│   ├── dao/                     # Acceso directo a MongoDB mediante Mongoose
│   ├── middlewares/             # Middlewares personalizados
│   ├── models/                  # Modelos y esquemas de Mongoose
│   ├── repositories/            # Abstracción del acceso a datos
│   ├── routes/                  # Definición de endpoints
│   ├── services/                # Lógica de negocio
│   └── utils/                   # Funciones auxiliares
│
├── app.js                       # Configuración de Express e inicialización de Passport
├── .env.example                 # Variables de entorno de ejemplo
├── .gitignore
├── package.json
├── package-lock.json
└── README.md
```

# Arquitectura

La aplicación implementa una arquitectura por capas:

## Arquitectura por capas

```text
Routes
   │
   ▼
Middlewares / Passport
   │
   ▼
Controllers
   │
   ▼
Services
   │
   ▼
Repositories
   │
   ▼
DAO
   │
   ▼
Models (Mongoose)
   │
   ▼
MongoDB
```

---
# Cada capa posee una responsabilidad específica:

Routes: define los endpoints de la API.
Middlewares: procesa validaciones y autenticación antes de acceder a determinadas rutas.
Controllers: recibe las solicitudes HTTP y genera las respuestas.
Services: contiene la lógica de negocio.
Repositories: abstrae el acceso a los datos.
DAO: gestiona las operaciones de persistencia.
Models: define los esquemas utilizados por Mongoose.
Utils: contiene funcionalidades reutilizables, como hash de contraseñas y JWT.
Config: contiene la configuración y conexión con la base de datos.

---

# Flujo completo de la autenticación
```
1. Registro
   │
   ▼
POST /api/sessions/register
   │
   ▼
Passport → register
   │
   ▼
Usuario creado
   │
   ▼
2. Login
   │
   ▼
POST /api/sessions/login
   │
   ▼
Passport → login
   │
   ▼
Credenciales válidas
   │
   ▼
Controller genera JWT
   │
   ▼
Cookie currentUser
   │
   ▼
3. Usuario actual
   │
   ▼
GET /api/sessions/current
   │
   ▼
Passport → current
   │
   ▼
JWT válido
   │
   ▼
req.user
   │
   ▼
Datos del usuario
   │
   ▼
4. Logout
   │
   ▼
POST /api/sessions/logout
   │
   ▼
Eliminación de currentUser
   │
   ▼
GET /api/sessions/current
   │
   ▼
401 No autenticado
```

# Autenticación
```
La autenticación de usuarios se encuentra centralizada mediante **Passport.js**, utilizando diferentes estrategias según la operación requerida:

- `register`: registro de nuevos usuarios.
- `login`: validación de credenciales.
- `current`: validación del JWT y recuperación del usuario autenticado.
```
# La cookie se configura con:
```
httpOnly: true
sameSite: lax
maxAge: 3600000
secure: true únicamente en producción
```

# El JWT contiene únicamente la información necesaria para identificar al usuario:
```
{
  "id": "665f2a...",
  "email": "ana@mail.com",
  "role": "user"
}
```

# Endpoints disponibles

## Eventos - GET

```http
GET /api/events/health
```

Obtiene el listado inicial de eventos.

Respuesta:

```json
{
  "status": "ok",
  "message": "API corriendo con éxito!"
}
```

---

## Events - GET

```http
GET /api/events
```

Obtiene el listado inicial de eventos.

Respuesta:

```json
{
  "status": "success",
  "payload": []
}
```

---

## Events - POST

```http
POST /api/events
```
Crea un nuevo evento.

Respuesta:

```json
{
  "status": "success",
  "messages": "Evento creado con éxito!"
}
```
---

## Events - POST

```http
POST /api/sessions/register
```
Registra un nuevo usuario.

Request:

```json
{
  "first_name": "Ana",
  "last_name": "Gómez",
  "email": "ana@mail.com",
  "password": "Secreta123",
  "role": "user"
}
```
Response - 201:

```json
{
  "status": "success",
  "payload": {
    "id": "665f2a...",
    "first_name": "Ana",
    "last_name": "Gómez",
    "email": "ana@mail.com",
    "role": "user"
  }
}
```
La contraseña se almacena mediante bcrypt y no se devuelve en la respuesta.

## Email duplicado — 409
Response - 409:

```json
{
  "status": "error",
  "message": "Email already in use"
}
```
---

## Login

Request:

```json
{
  "email": "ana@mail.com",
  "password": "Secreta123"
}
```
Response - 200:

```json
{
  "status": "success",
  "message": "Login successful"
}
```
---

## Current

Response - 200:

```json
{
  "status": "success",
  "payload": {
    "id": "6a74863134fabcebe6a74ad3",
    "email": "lilu@gmail.com",
    "role": "user"
  }
}
```
---

## Logout

Response - 200:

```json
{
  "status": "success",
  "message": "Sesión cerrada"
}
```
---


Luego del logout, una solicitud a /api/sessions/current devuelve:

Response — 401
```json
{
  "status": "error",
  "message": "No autenticado"
}
```

# Próximas funcionalidades

- Implementación de Passport.
- Gestión de roles.
- CRUD de eventos.
- Inscripción de usuarios.
- Control de cupos.
- Notificaciones.

---

# Evidencias

## Servidor en ejecución

![Servidor ejecutándose](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1784654404/serverRun_bwrwnt.png)

---

## Endpoint Events

![Endpoint Events GET](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1784654231/rutaEvent_apyvgn.png)
![Endpoint Events POST](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1785178601/postEvents_nctbvp.png)
---

## Endpoint Sessions

![Endpoint Sessions-Register POST](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1786210829/login_dexrin.png)
![Endpoint Sessions-Current GET](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1786210855/current_ppis22.png)
![Endpoint Sessions-Logout POST](https://res.cloudinary.com/dcfo0mbfj/image/upload/v1786210871/logout_k8ofbi.png)
---

# Notas

- El proyecto utiliza **ES Modules** (`"type": "module"` en `package.json`).
- La autenticación se encuentra centralizada mediante Passport.js.
- Las estrategias register, login y current se encuentran centralizadas en src/config/passport.config.js.
- Passport se inicializa en app.js mediante passport.initialize().
- Passport valida las credenciales y el JWT, mientras que el controller es responsable de generar el JWT y establecer la cookie después de un login exitoso.
- La contraseña nunca se devuelve en las respuestas de la API.
- El logout elimina la cookie currentUser y no requiere autenticación mediante Passport.
- La aplicación utiliza una arquitectura por capas para facilitar el mantenimiento y la incorporación de nuevas funcionalidades.
- La estructura queda preparada para incorporar futuras estrategias de autenticación mediante providers externos, como Google o GitHub, sin modificar app.js.
- El proyecto sigue una arquitectura por capas para facilitar su evolución durante el curso.